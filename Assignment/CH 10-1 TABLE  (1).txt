CREATE BELOW TABLES WITHOUT USING DEFAULT AND CONSTRAINTS :        // OKY LET'S DO IT

1. CREATE TABLE TO STORE BELOW INFO.
   FACULTIES : FACULTY_NO, FACNM, JOINDATE, SALARY
   
   CREATE TABLE FACULTIES(FACULTY_NO NUMBER,FACNM VARCHAR(30),JOINDATE DATE,SALARY NUMBER);
	
2. CREATE TABLE TO STORE BELOW INFO.
   SUBJECTS : SUBJECT_NO, SUBNM, TOTAL_DURATION_IN_HOURS
   
   CREATE TABLE SUBJECTS(SUBJECT_NO INT,SUBNM VARCHAR(30),TOTAL_DURATION_IN_HOURS INTERVAL DAY TO SECOND); // INT DONGE TOH BHI NUMBER HI HUA WO BHI

3. CREATE TABLE TO STORE BELOW INFO.                                   // MTLB INT YA NUMBER KUCH BHI DE SKTE HAI!
   FACULTY_SUBJECT : FACULTY_NO, SUBJECT_NO

   CREATE TABLE FACULTY_SUBEJCT(FACULTY_NO INT,SUBJECT_NO INT);

4. CREATE TABLE TO STORE BELOW INFO.
   COURSE : COURSE_NO, COURSE_NAME, DURATION_IN_MONTHS, GENERAL_CLUB, TOTAL_FEES
   
   CREATE TABLE COURSE(COURSE_NO INT,COURSE_NAME VARCHAR(30),DURATION_IN_MONTHS INTERVAL YEAR TO MONTHS,TOTAL_FEES INT,GENERAL_CLUB VARCHAR(10));

5. CREATE TABLE TO STORE BELOW INFO.
   STUDENTS : STUDENT_NO, STUNM, BIRTH_DATE, COURSE_NO

   CREATE TABLE STUDENTS(STUDENT_NO INT,STUNM VARCHAR(30),BIRTH_DATE DATE,COURSE_NO INT);

6. CREATE TABLE TO STORE BELOW INFO.
   FEES : RECEIPT_NO, RECDT, AMOUNT, CASH_OR_CHEQUE, CHEQUE_NO, CHEQUE_DATE, BANK_NAME

   CREATE TABLE FEES(RECEIPT_NO INT,BANK_NAME VARCHAR(30),RECDT DATE,AMOUNT INT,CHEQUE_DATE DATE,CHEQUE_NO INT,CASH_OR_CHEQUE VARCHAR(10));

7. CREATE TABLE TO STORE BELOW INFO.
   MARKS : STUDENT_NO, SUBJECT_NO, FACULTY_NO, MARKS_SCORED

   CREATE TABLE MARKS(STUDENT_NO INT,SUBJECT_NO INT,FACULTY_NO INT,MARKS_SCORED INT);

8. CREATE TABLE TO STORE BELOW INFO.
   BATCH_MASTER : BATCH_NO, FACULTY_NO, SUBJECT_NO, START_DT, DURATION_IN_DAYS

   CREATE TABLE BATCH_MASTER(BATCH_NO INT,DURATION_IN_DAYS INTERVALS DAYS TO SECONDS,START_DT DATE,SUBJECT_NO INT,FACULTY_NO INT);

9. CREATE TABLE TO STORE BELOW INFO.
   BATCH_DETAILS : BATCH_NO, STUDENT_NO

   CREATE TABLE BATCH_DETAILS(BATCH_NO INT,STUDENT_NO INT);

10. CREATE TABLE TO STORE BELOW INFO.
    BATCH_ATTENDANCE : BATCH_NO, STUDENT_NO , LECTURE_DATE, PRESENT_STATUS

   CREATE TABLE BATCH_ATTENDANCE(BATCH_NO INT,STUDENT_NO INT,LECTURE_DATE DATE,PRESENT_STATUS VARCAR(4));   